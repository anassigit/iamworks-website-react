spring.application.name=iamworks-backend
server.port=${PORT:${SERVER_PORT:8080}}
spring.profiles.default=local

app.cors.allowed-origin-patterns=${APP_CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:*,http://127.0.0.1:*,https://anassigit.github.io}

spring.jpa.open-in-view=false
spring.jpa.show-sql=false
